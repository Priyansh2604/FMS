# Finance App Frontend

This folder will hold the React frontend for the Finance Management System.

To create the frontend app, run:

```bash
npx create-react-app frontend
```

After creation, use the `frontend/src` folder to build the UI.
